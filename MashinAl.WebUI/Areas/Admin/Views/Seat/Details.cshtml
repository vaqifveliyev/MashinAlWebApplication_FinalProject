@model Seats
@{
    ViewData["Title"] = Model.SeatTitle + " | Ətraflı";
}


<div class="card">
    <div class="card-body">
        <h5 class="card-title fw-semibold mb-4">@Model.SeatTitle | Ətraflı </h5>
        <div class="card">
            <div class="card-body">
                <form>
                    <div class="mb-3">
                        <label class="form-label">Yer sayı</label>
                        <input value="@Model.SeatTitle" class="form-control" readonly>
                    </div>
                    <a asp-action="index" class="btn btn-primary">Geri</a>
                    <a asp-action="edit" asp-route-id="@Model.Id" class="btn btn-warning">Dəyişiklik et</a>
                </form>
            </div>
        </div>
    </div>
</div>



